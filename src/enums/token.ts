export const SESSION_DURATION = "7d" as const;

export const SESSION_ID_COOKIE = "session-id" as const;
export const CSRF_TOKEN_COOKIE = "csrf-token" as const;
export const CSRF_TOKEN_HEADER = CSRF_TOKEN_COOKIE;
